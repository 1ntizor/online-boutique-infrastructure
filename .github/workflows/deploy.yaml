name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  deploy:

    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: YAML Lint
        uses: iblancasa/check-yaml@v1
        with:
          files:  k8s-manifests/

